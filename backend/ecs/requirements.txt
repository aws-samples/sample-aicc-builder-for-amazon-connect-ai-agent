# ECS Fargate deployment dependencies
# No bedrock-agentcore, no redis

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0

# AI Agent SDK
strands-agents>=1.22.0
strands-agents-tools>=0.1.0

# Web search via Amazon Bedrock AgentCore Gateway (MCP over SigV4, no API key)
mcp>=1.9.0
mcp-proxy-for-aws>=1.6.2

# AWS SDK
boto3>=1.35.0

# Data validation
pydantic>=2.0.0

# Asset linting (deterministic post-generation validation gates)
cfn-lint>=1.0.0
openapi-spec-validator>=0.7.0

# Configuration
PyYAML>=6.0.0

# Auth
python-jose[cryptography]>=3.3.0

# Database drivers (for introspect_database tool)
# Every RDS/Aurora engine is reachable: Aurora uses the RDS Data API when its
# HTTP endpoint is enabled, everything else connects with one of these drivers.
PyMySQL>=1.1.0            # MySQL, MariaDB, Aurora MySQL
psycopg2-binary>=2.9.0    # PostgreSQL, Aurora PostgreSQL
python-tds>=1.15.0        # SQL Server (pure Python, no FreeTDS needed)
oracledb>=2.4.0           # Oracle (thin mode, no Instant Client needed)

# HTTP client
requests>=2.31.0

# Observability
aws-xray-sdk>=2.14.0

# CloudWatch metrics
watchtower>=3.0.0

# Document parsers (for File Explorer binary file support)
python-docx>=1.1.0
PyPDF2>=3.0.0
openpyxl>=3.1.0
