{
  "properties": {
    "name": {
      "title": "Name",
      "type": "string"
    },
    "trigger": {
      "default": "input",
      "description": "'input' or 'output'",
      "title": "Trigger",
      "type": "string"
    },
    "policy": {
      "description": "The business policy, in plain language",
      "title": "Policy",
      "type": "string"
    },
    "detection_method": {
      "default": "auto",
      "description": "'regex', 'keyword', 'llmJudge' or 'auto'",
      "title": "Detection Method",
      "type": "string"
    },
    "action": {
      "default": "flag",
      "description": "'mask', 'modify', 'route' or 'flag'",
      "title": "Action",
      "type": "string"
    },
    "route_flow_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Route Flow Id"
    },
    "examples": {
      "items": {
        "type": "string"
      },
      "title": "Examples",
      "type": "array"
    },
    "message": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The sentence to say VERBATIM when the rule fires, copied from the requirements (e.g. an emergency notice before the hand-off). A route rule with a message is also written into every journey's rules, so the caller hears it wherever the trigger words are said",
      "title": "Message"
    }
  },
  "required": [
    "name",
    "policy"
  ],
  "title": "ACXDGuardrailPlan",
  "type": "object"
}