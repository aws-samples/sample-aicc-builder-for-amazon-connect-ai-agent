{
  "description": "One planned step of a flow, with its determinism decision.",
  "properties": {
    "step": {
      "description": "1-based order within the flow",
      "title": "Step",
      "type": "integer"
    },
    "description": {
      "description": "What happens at this step, in plain language",
      "title": "Description",
      "type": "string"
    },
    "node_type": {
      "description": "ACXD node type; one of ['basic', 'choice', 'data_request', 'define', 'end', 'escalate', 'generative_journey', 'generative_task', 'generative_text', 'intent_capture', 'knowledge_base', 'loop', 'note', 'redirect', 'split', 'start', 'transform', 'user_choice', 'user_input', 'wait']",
      "title": "Node Type",
      "type": "string"
    },
    "determinism": {
      "default": "deterministic",
      "description": "'deterministic' (fixed) or 'generative' (LLM-driven)",
      "title": "Determinism",
      "type": "string"
    },
    "determinism_rationale": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Why this label was recommended, in plain language",
      "title": "Determinism Rationale"
    },
    "decision_category": {
      "default": "general",
      "description": "What the step decides; one of ('authorization', 'compliance', 'eligibility', 'identity', 'money', 'payment', 'refund', 'general'). Money/refund/payment/authorization/eligibility/compliance/identity steps are always deterministic.",
      "title": "Decision Category",
      "type": "string"
    },
    "data_request_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "For data_request steps: the Data Request to call. Defaults to the flow's operation_id (one Data Request per operation).",
      "title": "Data Request Id"
    },
    "redirect_flow_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "For redirect steps: the flow_id this step hands the conversation to (e.g. the customer-info search flow when the order number is not found). The generated flow must redirect there; system hand-offs (follow-up, escalation) need not be named.",
      "title": "Redirect Flow Id"
    },
    "captures": {
      "description": "generative_journey steps only: names of the flow's slots the journey collects in conversation (e.g. ['acType', 'quantity', 'phoneNumber']). A journey that carries the operation (journey_tools names a data_request) collects every value, strict-format ones included \u2014 it reads them back and re-asks on a wrong shape. Only a journey WITHOUT a data request leaves strict-format values (a regex, a phone number, an identifier) to their own user_choice steps, which the runtime checks character by character.",
      "items": {
        "type": "string"
      },
      "title": "Captures",
      "type": "array"
    },
    "slot": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "user_choice steps: the flow slot this step collects (e.g. 'orderNumber', or the yes/no slot of a consent gate).",
      "title": "Slot"
    },
    "journey_tools": {
      "description": "generative_journey steps only. 'data_request' gives the journey the operation's own Data Request as a tool (the recommended shape: the journey collects, calls the backend and announces the result itself); 'data_request:<id>' adds another bundled request (a price lookup before a booking, a search by customer info after a miss); 'knowledge_base' lets it answer side questions from the FAQ knowledge base.",
      "items": {
        "type": "string"
      },
      "title": "Journey Tools",
      "type": "array"
    },
    "template": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "The exact sentence the customer approved for this step, with placeholders: for a basic / generative_text result step the announcement (\"\uc608\uc57d\uc774 \uc811\uc218\ub418\uc5c8\uc2b5\ub2c8\ub2e4. \uc608\uc57d\ubc88\ud638\ub294 {createReservation.reservationId:NLX.Variable}\uc774\uba70 \u2026\" \u2014 for a generative_text it is the deterministic fallback spoken when the workspace has no model), for a generative_journey the read-back of the captured values. Propose one in the customer's register, ask once whether to keep it or change it, and store what they approved; the generator uses it verbatim.",
      "title": "Template"
    },
    "user_confirmed": {
      "default": false,
      "title": "User Confirmed",
      "type": "boolean"
    },
    "confirmation_pending_reason": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Set when a re-upsert changed the decision and reset confirmation",
      "title": "Confirmation Pending Reason"
    }
  },
  "required": [
    "step",
    "description",
    "node_type"
  ],
  "title": "ACXDNodeStep",
  "type": "object"
}