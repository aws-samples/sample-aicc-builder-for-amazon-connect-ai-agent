# === Generated / Deploy Outputs ===
cdk-outputs.json
cdk-outputs-*.json
frontend/.env
node_modules/
chat

# Root-level npm artifacts (none are required; harness-only packages)
/package.json
/package-lock.json
/pnpm-lock.yaml

# === AgentCore build artifacts ===
backend/agentcore/.bedrock_agentcore/
backend/agentcore/.bedrock_agentcore.yaml
backend/agentcore/src/

# === Demo & docs (managed separately) ===
CLAUDE.md
demo/
debug/
improvements/
refactoring-to-ecs-s3files/
PATCH_NOTES.md

# === Internal-only docs (never publish to the public sample repo) ===
# Internal PM backlog, QA execution logs, and anything referencing live
# dev endpoints / instance ids. Kept on local disk under internal-docs/.
internal-docs/

# === Workshop content & static assets ===
assets/*
!assets/README.md
static/
content/

# === Environment & secrets ===
.env
.env.local
.env.*.local
# Backups of a generated .env.local still contain real Cognito/API identifiers.
.env.local.bak
.env*.bak

# === Work in Progress ===


# === Frontend ===
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/

# === Backend ===
backend/.venv/
backend/**/.venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.pyc
backend/**/*.pyc
backend/.pytest_cache/
backend/**/.pytest_cache/
backend/.mypy_cache/
backend/.ruff_cache/
backend/dist/
backend/*.egg-info/
backend/**/*.egg-info/

# === Infrastructure ===
infrastructure/node_modules/
infrastructure/cdk.out/
infrastructure/cdk.context.json
infrastructure/dist/
infrastructure/*.js
infrastructure/*.d.ts
!infrastructure/jest.config.js

# === IDE ===
.idea/
.vscode/
.kiro/
*.swp
*.swo

# === OS ===
.DS_Store

# === Logs ===
*.log
npm-debug.log*

# === Deploy cache ===
.deploy-hashes/

# === Large media ===
*.mov
*.mp4

# === Misc generated files ===
gamma/
blog/
updates/
questionnaire/*.docx
*_tmp.html
**/__pycache__/
# Playwright MCP test artifacts (console logs, page snapshots)
.playwright-mcp/

# Playwright run artifacts (reports, videos, screenshots — throwaway test output)
frontend/playwright-report/
frontend/test-results/
frontend/e2e/.auth/

# The Playwright e2e harness stays LOCAL — it is not part of the published
# sample. It drives our deployed dev environment and carries environment
# specifics (URLs, saved Cognito state, transcripts of real runs) that must not
# land in the public repo. Keep it out of git entirely rather than trying to
# scrub it per-file.
frontend/e2e/
frontend/playwright.config.ts
frontend/.env.e2e

.e2e-tmp/

# QA / demo capture artifacts (screenshots, demo bundles) — not source
demo-assets/
qa-*.png
slack-*.png
investigate-*.png
debug/
scripts/.websearch-venv/
